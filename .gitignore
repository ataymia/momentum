# dependencies
node_modules
.pnp
.pnp.js

# builds
.next
out
dist
tsconfig.tsbuildinfo
*.tsbuildinfo

# runtime / tooling caches
.wrangler
.sites-runtime
.firebase

# local env
# NOTE: .env.production is intentionally committed. Firebase web config is public by design and
# carries no authority; access is enforced by firestore.rules. Real secrets belong in .env.local.
.env
.env.local
.env*.local

# editor / os
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*


# emulator
firestore-debug.log
ui-debug.log
