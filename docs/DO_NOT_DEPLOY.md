# Deployment hold released (2026-09-15)

This branch was validation-first. The hold is now lifted: the full gate (`rules:build`, `typecheck`,
`lint`, `test:logic`, `test:rules`, `build`) passes and the release owner approved the production
deployment of the onboarding rescue, Brand Ambassador, training administration, and Firebase Storage work.

Reinstate this hold for the next change set. See `docs/PRODUCTION_RELEASE_RULE.md`.
