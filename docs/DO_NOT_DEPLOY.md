# Do not deploy before validation

This branch contains production-facing fixes but is intentionally validation-first. Do not deploy it directly. Merge only after CI and role-based smoke testing pass and the release owner approves production deployment.
