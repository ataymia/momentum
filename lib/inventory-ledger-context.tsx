"use client";

/** Stable import path for the inventory-ledger context. `-v2` is the current implementation. */
export { InventoryLedgerProvider, useInventoryLedger } from "./inventory-ledger-context-v2";
